version: '2'

services:

  myapp:
    build: .
    ports:
      - 8080:8080
  db:
    image: mysql
    restart: always
    ports:
      - 3306:3306
    environment:
        MYSQL_DATABASE: "users"
        MYSQL_ROOT_PASSWORD: hao825_MDL7519